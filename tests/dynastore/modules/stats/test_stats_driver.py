"""Stats driver tests.

The PostgreSQL stats driver was removed in v1.0. Stats are now stored in
Elasticsearch via ElasticsearchStatsDriver. These tests require an ES instance
and are covered by integration tests.
"""
