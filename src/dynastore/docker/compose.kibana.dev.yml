# Dev override for kibana: expose 5601 so the Dashboards UI is reachable
# directly from the host (bypassing the reverse-proxy during development).

services:
  kibana:
    extends:
      file: compose.kibana.yml
      service: kibana
    ports:
      - "5601:5601"
