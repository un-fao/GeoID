#    Copyright 2025 FAO — Apache 2.0; see ../../LICENSE.

[project]
name = "dynastore-ext-proxy"
dynamic = ["version"]
description = "DynaStore Proxy extension — outbound HTTP proxy with tenant scoping."
requires-python = ">=3.12"
license-files = ["../../../LICENSE"]
dependencies = [
    "fastapi>=0.100",
    "DynaStore",
]

[project.entry-points."dynastore.extensions"]
proxy = "dynastore.extensions.proxy.proxy_service:ProxyService"

[build-system]
requires = ["setuptools>=80.9.0", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.uv.sources]
dynastore = { workspace = true }

[tool.setuptools_scm]
version_file = "src/dynastore/extensions/proxy/_scm_version.py"
local_scheme = "no-local-version"
fallback_version = "0.0.0+unknown"
