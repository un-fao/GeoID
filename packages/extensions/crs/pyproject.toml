#    Copyright 2025 FAO — Apache 2.0; see ../../LICENSE.

[project]
name = "dynastore-ext-crs"
dynamic = ["version"]
description = "DynaStore CRS extension — coordinate reference system registry."
requires-python = ">=3.12"
license-files = ["../../../LICENSE"]
dependencies = [
    "fastapi>=0.100",
    "pyproj>=3.0.0",
    "DynaStore",
]

[project.entry-points."dynastore.extensions"]
crs = "dynastore.extensions.crs.crs_extension:CRSExtension"

[build-system]
requires = ["setuptools>=80.9.0", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.uv.sources]
dynastore = { workspace = true }

[tool.setuptools_scm]
version_file = "src/dynastore/extensions/crs/_scm_version.py"
local_scheme = "no-local-version"
fallback_version = "0.0.0+unknown"
